<nav class="navbar navbar-expand-lg navbar-dark bg-dark">
    <div class="container-fluid">
        <a class="navbar-brand" href="{{path('homepage')}}">
            <font style="vertical-align: inherit;">
                <font style="vertical-align: inherit;">MyBooking</font>
            </font>
        </a>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarColor01" aria-controls="navbarColor01" aria-expanded="false" aria-label="Basculer la navigation">
            <span class="navbar-toggler-icon"></span>
        </button>

        <div class="collapse navbar-collapse" id="navbarColor01">
            <ul class="navbar-nav me-auto">
                <li class="nav-item">
                    <a class="nav-link active" href="{{path('admin_dashboard')}}">
                        <font style="vertical-align: inherit;"><i class="fas fa-chart-line"></i> Tableau de bord</font>
                    </a>
                </li>

                <li class="nav-item">
                    <a class="nav-link active" href="{{path('admin_ads_list')}}">
                        <font style="vertical-align: inherit;"><i class="fas fa-hotel"></i> Nos annonces</font>
                    </a>
                </li>

                <li class="nav-item">
                    <a class="nav-link active" href="{{path('admin_bookings_list')}}">
                        <font style="vertical-align: inherit;"><i class="fas fa-folder"></i> Réservations</font>
                    </a>
                </li>

                <li class="nav-item">
                    <a class="nav-link active" href="{{path('admin_comments_list')}}">
                        <font style="vertical-align: inherit;"><i class="fas fa-comments"></i> Commentaires</font>
                    </a>
                </li>

                <li class="nav-item">
                    <a class="nav-link active" href="#">
                        <font style="vertical-align: inherit;"><i class="fas fa-users"></i> Utilisateurs</font>
                    </a>
                </li>
            </ul>

            <ul class="navbar-nav ml-auto align-items-center">

            {% if app.user %}

            <li class="nav-item">
                <a href="{{path('homepage')}}" class="nav-link" target="_blank"><i class="fas fa-eye"></i> Voir le site</a>
            </li>

                <div class="dropdown">
                     <button class="btn btn-dark dropdown-toggle" type="button" id="accountDropdownLink" data-bs-toggle="dropdown" aria-expanded="false">
                        <img src="{{ app.user.avatar}}" alt="Avatar de {{ app.user.fullName }}" class="avatar avatar-small">
                         {{ app.user.fullName}}
                     </button>

                     <ul class="dropdown-menu" aria-labelledby="accountDropdownLink">
                        <li><a class="dropdown-item" href="{{path('admin_account_logout')}}">Déconnexion</a></li>
                     </ul>
                </div>
                
            {% else %}
                <li class="nav-item">
                    <a class="nav-link active" href="{{path('account_register')}}">
                        <font style="vertical-align: inherit;">
                            <font style="vertical-align: inherit;">Inscription</font>
                        </font>
                    </a>
                </li>

                <li class="nav-item">
                    <a class="nav-link active" href="{{path('account_login')}}">
                        <font style="vertical-align: inherit;">
                            <font style="vertical-align: inherit;">Connexion</font>
                        </font>
                    </a>
                </li>
            {% endif %}

            </ul>
        </div>
    </div>
</nav>